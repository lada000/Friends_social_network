<h1>Users with Matching Categories</h1>

<% if @matches.present? %>
  <ul>
    <% @matches.each do |match| %>
      <li><%= match.name %> - Shared Categories: <%= match.shared_categories_count %></li>
    <% end %>
  </ul>
<% else %>
  <p>No matches found with 5 or more shared categories.</p>
<% end %>